import-meta-resolve.ts:1:18 - error SC2020: 'ImportMeta.resolve' is part of the standard library types but has no scriptc lowering yet

  1 | const resolved = import.meta.resolve("./module.mjs");
    |                  ^~~~~~~~~~~~~~~~~~~
  2 | console.log(resolved);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)